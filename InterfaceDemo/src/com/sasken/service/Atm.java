package com.sasken.service;

public abstract interface Atm {		// interface itself by default abstract
	public abstract void withDraw(); // method itself by default abstract
	public abstract void getBalance();
}
